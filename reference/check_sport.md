# Checks if sport is valid

Checks if sport is valid

## Usage

``` r
check_sport(sport = NULL, vb_only = TRUE)
```

## Arguments

- sport:

  Sport code.

- vb_only:

  Logical indicating whether to check only for volleyall sports (TRUE)
  or all sports (FALSE)
